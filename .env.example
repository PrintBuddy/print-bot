TELEGRAM_TOKEN=
# Shared secret sent as the X-Telegram-Secret header on every request to the
# backend's /telegram/* routes. Must match the backend's own TELEGRAM_SECRET.
TELEGRAM_SECRET=

API_BASE_URL=http://localhost:8000/api
API_TIMEOUT=5

# Optional: not currently used to gate any command locally, only kept for
# reference/future use.
ADMIN_CHAT_ID=
